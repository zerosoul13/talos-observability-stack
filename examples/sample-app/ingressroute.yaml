apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sample-app
  namespace: default
  labels:
    app: sample-app
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`app.local.dev`)
      kind: Rule
      services:
        - name: sample-app
          port: 8080
